# 반도체 뉴스 스크랩 사이트 — 프로젝트 스펙 (v2)

## 0. 업무 재정의 관점
- **Before**: 매일 아침 전자신문 / ZDNet Korea / 디일렉 등 사이트를 각각 직접 접속해
  반도체 관련 기사를 눈으로 훑어 골라내는 방식. 소요 시간이 들쭉날쭉하고,
  바쁜 날엔 확인을 건너뛰어 놓치는 기사가 생김.
- **After**: RSS 수집 → AI 관련도 스코어링·요약 → 자동 정렬된 카드 페이지로 재설계.
  사람은 "결과를 열람하고 판단"하는 역할만 남고, 수집·1차 필터링·요약은
  에이전트가 수행.
- **재정의 포인트**: 이 프로젝트는 단순히 "뉴스 사이트를 만든 것"이 아니라,
  "산업 동향 모니터링이라는 개인 업무를 AI 에이전트에게 위임 가능한 형태로
  재설계한 것"이 핵심. spec.md 자체가 업무를 정의해 에이전트(Claude Code)에게
  위임하기 위한 작업지시서 역할을 함.

## 1. 목적
반도체 산업 관련 뉴스/이슈를 매일 자동으로 수집·판단·요약해서, 하루 한 번
가볍게 훑어볼 수 있는 정적 웹사이트를 만든다. 삼성전자 메모리 사업부
공정기술 직무 취업 준비 과정에서 산업 동향을 꾸준히, 효율적으로 파악하는
용도로 사용한다.

## 2. 핵심 요구사항
1. 지정된 소스에서 매일 자동으로 기사를 수집한다.
2. 수집된 항목을 규칙 기반으로 "기사 / 회사 발표 / 기타"로 분류한다.
3. **각 항목을 AI(Claude API)에 전달해 요약과 관련도 스코어링을 수행한다.** (5절 참고)
4. 분류·요약·스코어 결과를 JSON으로 저장한다.
5. JSON을 읽어 카테고리별·관련도순 카드 UI로 보여주는 정적 페이지를 만든다.
   카드를 클릭하면 원문 사이트로 새 탭 이동한다.
6. 매일 자동 갱신되도록 GitHub Actions cron 워크플로우를 작성한다.
7. GitHub Pages로 배포 가능한 구조로 만든다.
8. 매일 수집 지표(기사 수, 고관련도 비율 등)를 로그로 남긴다. (8절 참고)

## 3. 수집 소스 (1차)
| 매체 | 유형 | 비고 |
|---|---|---|
| 전자신문 (etnews.com) | 기사 | 기사별 RSS 제공, 반도체 외 산업도 섞여 있어 키워드 필터링 필요 |
| ZDNet Korea 반도체 섹션 (zdnet.co.kr) | 기사 | 반도체 섹션 전용 RSS 존재 (URL 유효성은 실행 시 확인) |
| 디일렉 THE ELEC (thelec.kr) | 기사 | 반도체/디스플레이/배터리 전문지, RSS 서비스 제공 |

### 확장 후보 (2차, 안정화 후 추가)
- 삼성 뉴스룸 (news.samsung.com) — 회사 발표, RSS 미확인 → 페이지 파싱 필요 가능성
- SK하이닉스 뉴스룸 — 회사 발표, RSS 미확인 → 페이지 파싱 필요 가능성
- 디지털데일리, 비즈니스포스트, EE Times Korea — 기사, RSS 여부 개별 확인 필요

## 4. 분류 기준 (규칙 기반, 1차 필터)
- **기사**: 언론사가 작성한 보도/분석 기사
- **회사 발표**: 기업 뉴스룸/보도자료 형태의 공식 발표
- **기타**: 위 두 범주에 속하지 않는 항목 (컨퍼런스, 채용 공고성 뉴스 등)

키워드 필터링 예시(전자신문처럼 종합지인 경우): "반도체", "메모리", "파운드리",
"D램", "낸드", "웨이퍼", "공정", "HBM" 등이 제목/본문에 포함된 기사만 수집.

## 5. AI 활용 레이어 (핵심 — 이 프로젝트의 에이전트화 지점)
규칙 기반 분류는 1차 필터일 뿐이고, 실제 "이 기사가 나에게 왜 중요한가"를
판단하는 역할은 AI가 맡는다.

- **사용 모델**: Claude API (Claude Sonnet 계열)
- **입력**: 기사 제목 + RSS description(또는 본문 일부)
- **출력 (구조화 JSON)**:
  - `summary`: 2~3문장 한글 요약
  - `relevance_score`: 1~5점, "삼성전자 메모리 사업부 공정기술 직무" 관점에서의 관련도
  - `relevant_tags`: 공정/소자/장비/시장/정책 등 태그 배열
- **프롬프트 설계 원칙**: 시스템 프롬프트에 "이 기사가 반도체 공정기술
  직무 준비생 입장에서 왜 중요한지" 관점을 명시적으로 주입해, 단순 요약이
  아니라 직무 맥락화된 판단을 하도록 유도한다.
- **실패 처리(fallback)**: API 호출 실패 시 요약·스코어 없이 원문 제목/링크만
  노출하고 `ai_processed: false`로 표시. 파이프라인 전체가 멈추지 않도록 한다.
- **경험기술서 관점**: 이 레이어가 "AI를 코드 생성 도구로 한 번 쓰고 끝"이
  아니라 "매일 반복 실행되는 파이프라인 안에서 AI가 지속적으로 판단을
  수행하는 에이전트"라는 서사의 핵심 근거가 된다.

## 6. 데이터 스키마 (JSON)
```json
{
  "collected_at": "2026-08-24T07:00:00+09:00",
  "items": [
    {
      "title": "기사 제목",
      "url": "https://...",
      "source": "전자신문",
      "category": "기사",
      "published_at": "2026-08-24T06:30:00+09:00",
      "summary": "AI가 생성한 2~3문장 요약",
      "relevance_score": 4,
      "relevant_tags": ["공정", "메모리"],
      "ai_processed": true
    }
  ]
}
```

## 7. 프론트엔드
- 정적 페이지 (React + Vite), 위 JSON을 읽어 섹션별로 렌더링
- **섹션 구성 (구현 시 확정, `category` 필드 그대로 쓰지 않음)**:
  1. 삼성전자 · SK하이닉스 — 제목/요약에 두 회사명이 언급된 기사 (`category`가
     "기타"가 아닌 항목 중 매칭)
  2. 기타 반도체 기업 — 위에 해당하지 않는 나머지 기사/회사 발표
  3. 기타 — `category`가 "기타"인 항목
  - 이렇게 바꾼 이유: 실사용자(취준생 본인) 입장에서 지원 기업(삼성전자/SK하이닉스)
    관련 소식과 그 외 반도체 업계 소식을 구분해서 보는 게 실질적으로 더 유용했음.
    회사 매칭 로직은 `web/src/lib/companyMatch.ts` 참고.
  - 각 섹션은 기본 6개만 표시, 초과분은 "더보기" 버튼으로 펼침
- 카드 구성: 제목 + 출처 + 발행 시각 + AI 요약 + 관련도 배지(1~5) + 태그
- 기본 정렬: 관련도(relevance_score) 내림차순, 섹션 내에서 최신순 토글 가능
- 카드 클릭 시 원문 URL로 새 탭 이동
- 최소 기능이므로 화려한 디자인보다 가독성 우선

## 8. 성과 측정 / Before-After 기록
경험기술서에 정량 지표로 쓰기 위해, 아래를 프로젝트 진행 중 직접 기록한다.

- **Before 측정**: 프로젝트 시작 전 3일간, 기존 방식(사이트 직접 순회)으로
  반도체 뉴스를 확인하는 데 걸리는 시간을 스톱워치로 측정해 평균값 기록
- **After 측정**: 자동화 완료 후 3일간, 사이트 접속 후 필요한 정보를
  확인하는 데 걸리는 시간 측정
- **파이프라인 지표**: 매일 수집 기사 수, AI가 고관련도(4~5점)로 분류한
  기사 비율, fallback(ai_processed: false) 발생 빈도
- 위 지표는 별도 스프레드시트나 `logs/` 폴더에 날짜별로 남겨두고,
  경험기술서 작성 시 표/그래프로 정리해 활용한다.

## 9. 자동화 / 배포
- 수집 스크립트는 Node.js 또는 Python (rss-parser / feedparser, 필요시
  cheerio로 페이지 파싱)
- AI 처리 단계는 Anthropic API 호출 (환경변수로 API 키 관리, 저장소에 커밋 금지)
- GitHub Actions: 매일 지정 시각(예: 한국시간 오전 7시)에 스크립트 실행 →
  AI 처리 → JSON 갱신 → 커밋 → 배포. 실행 로그를 8절 지표 기록에도 활용
- 배포처: GitHub Pages (무료, 정적 사이트에 적합)

## 10. 진행 순서 제안
1. Before 측정 시작 (기존 방식 소요 시간 3일 기록)
2. 프로젝트 구조 및 기술 스택 제안받기
3. RSS 소스 3곳(전자신문, ZDNet 반도체, 디일렉) 연동 및 테스트
4. 규칙 기반 분류 로직 구현
5. AI 요약/스코어링 레이어 구현 및 fallback 처리
6. 프론트엔드 카드 UI 구현 (관련도 정렬 포함)
7. 로컬 실행 확인
8. GitHub Pages 배포
9. GitHub Actions cron 자동화 완성 (AI 처리 단계 포함)
10. After 측정 시작, 파이프라인 지표 로그 축적
11. (2차) 회사 뉴스룸 소스 추가
